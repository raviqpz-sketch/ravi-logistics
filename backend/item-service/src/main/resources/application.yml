server:
  port: 8081

spring:
  application:
    name: item-service
  datasource:
    url: jdbc:postgresql://localhost:5432/RAVIEXPRESS
    username: ravilogistics
    password: ravi@123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
  security:
    user:
      name: ravilogistics
      password: ravi@123

eureka:
  client:
    service-url:
      defaultZone: http://eureka:eureka123@localhost:8761/eureka/
  instance:
    prefer-ip-address: true
